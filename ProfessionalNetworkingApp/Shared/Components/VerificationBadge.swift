// VerificationBadge.swift

import SwiftUI

struct VerificationBadge: View {
    var body: some View {
        Image(systemName: "checkmark.seal.fill").foregroundColor(.blue)
    }
}

#Preview { VerificationBadge() }
