// UserCard.swift (deprecated)
// For backward compatibility during migration; prefer using UserProfile directly.

import Foundation

typealias UserCard = UserProfile

enum SwipeAction { case like, pass }
